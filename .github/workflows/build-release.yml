name: "Build & Release"

on:
  pull_request:
    branches: [master]
    paths:
      - "src/**/*.c"
      - "src/**/*.h"
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [master]
    paths:
      - "src/**/*.c"
      - "src/**/*.h"
  workflow_dispatch:
    # 新增：手动触发时的参数选择
    inputs:
      deploy_type:
        description: '部署类型'
        required: true
        default: 'artifact_only'  # 默认仅生成 Artifact
        type: choice
        options:
          - artifact_only        # 仅上传 Artifact（不发布 Release）
          - release              # 上传 Artifact + 发布到 GitHub Release

concurrency:
  group: "build-release"
  cancel-in-progress: false

jobs:
  build:
    name: Build on ${{ matrix.platform }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    env:
      PROJECT_NAME: ${{ github.event.repository.name }}

    strategy:
      matrix:
        include:
          # 原有 Linux 版本（glibc）
          - os: ubuntu-latest
            platform: Linux
            arch: x64
          - os: ubuntu-24.04-arm
            platform: Linux
            arch: arm64
          # 新增 Linux musl 版本（静态编译）
          - os: ubuntu-latest
            platform: Linux
            arch: x64-musl
          - os: ubuntu-24.04-arm
            platform: Linux
            arch: arm64-musl
          # 原有 macOS/Windows 版本（保持不变）
          - os: macos-15-intel
            platform: macOS
            arch: Intel
          - os: macos-latest
            platform: macOS
            arch: arm64
          - os: windows-latest
            platform: Windows
            arch: x64
          - os: windows-11-arm
            platform: Windows
            arch: arm64

    steps:
      - name: Checkout Git Repository
        uses: actions/checkout@v5

      - name: Setup Xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: ~3
          actions-cache-folder: ".xmake-cache"
          build-cache-key: "${{ matrix.platform }}_${{ matrix.arch }}"
          build-cache: true
          package-cache: true

      # 为 musl 架构安装 musl 工具链
      - name: Install Musl Toolchain (for musl arch)
        if: contains(matrix.arch, 'musl')
        run: |
          sudo apt update && sudo apt install -y \
            musl-tools \
            gcc-aarch64-linux-gnu \
            musl-dev \
            gcc-multilib
          # 为 arm64-musl 配置交叉编译工具链
          if [[ "${{ matrix.arch }}" == "arm64-musl" ]]; then
            sudo apt install -y aarch64-linux-gnu-musl-dev
            ln -s /usr/bin/aarch64-linux-gnu-gcc /usr/bin/aarch64-linux-musl-gcc || true
          fi

      # 调整构建命令：区分 musl/普通版本，配置 Xmake 静态编译
      - name: Build & Package Project
        run: |
          # 针对 musl 架构配置 Xmake 编译参数
          if [[ "${{ matrix.arch }}" == *-musl ]]; then
            # 设置 musl 工具链 + 静态链接
            xmake f \
              --plat=linux \
              --arch=${{ replace(matrix.arch, '-musl', '') }} \
              --toolchain=musl \
              --static=y \
              --mode=release
          else
            # 原有普通编译逻辑
            xmake f --mode=release
          fi
          # 执行构建并打包
          xmake build --verbose
          xmake package --yes --verbose

      - name: Upload Artifact
        # 条件：push 事件 / 手动触发（无论选择哪种部署类型都上传 Artifact）
        if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            build/packages/*.zip
            build/packages/*.tar.gz
          if-no-files-found: error
          retention-days: 7  # 延长 Artifact 保留时间（可选）
          compression-level: 9

  release:
    # 条件控制：
    # 1. push 事件：默认发布 Release（保留原有逻辑）
    # 2. 手动触发：仅当选择 deploy_type=release 时才执行
    if: ${{ (github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && inputs.deploy_type == 'release') }}
    name: Upload to GitHub Release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    env:
      RELEASE_DIR: "artifacts"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Download All Artifacts
        uses: actions/download-artifact@v5
        with:
          path: ${{ env.RELEASE_DIR }}

      - name: Update dev Tag
        run: |
          git tag dev ${{ github.sha }}
          git push origin "refs/tags/dev" --force
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev
          name: Continuous dev Build
          body: |
            Automated build from commit ${{ github.sha }}
            - Added Linux musl static build (x64/arm64)
            - Deploy type: ${{ github.event_name == 'push' && 'auto-release (push)' || inputs.deploy_type }}
          prerelease: true
          preserve_order: true
          files: |
            ${{ env.RELEASE_DIR }}/**/*.zip
            ${{ env.RELEASE_DIR }}/**/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  format:
    # 条件：仅当 Release 执行时才运行（依赖 release job）
    if: ${{ (github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && inputs.deploy_type == 'release') }}
    name: Auto Format Code
    needs: release
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref || github.ref_name }}  # 兼容 push/workflow_dispatch 事件

      - name: Setup Xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: ~3
          actions-cache-folder: ".xmake-cache"

      - name: Format C/C++ files
        run: xmake format

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet; then
            echo "changed=0" >> $GITHUB_OUTPUT
          else
            echo "changed=1" >> $GITHUB_OUTPUT
          fi

      - name: Auto commit and push changes
        if: steps.check_changes.outputs.changed != 0
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "style(format): AUTO format code"
          branch: ${{ github.ref_name }}
          commit_options: "--no-verify --no-signoff"
          file_pattern: "*.h *.c"
          commit_author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          status_options: "--untracked-files=no"
          add_options: "-u"
          disable_globbing: true