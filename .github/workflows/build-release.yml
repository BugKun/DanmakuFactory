name: "Build & Release"

on:
  pull_request:
    branches: [master]
    paths:
      - "src/**/*.c"
      - "src/**/*.h"
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [master]
    paths:
      - "src/**/*.c"
      - "src/**/*.h"
  workflow_dispatch:
    inputs:
      deploy_type:
        description: '部署类型'
        required: true
        default: 'artifact_only'
        type: choice
        options:
          - artifact_only
          - release

concurrency:
  group: "build-release"
  cancel-in-progress: false

jobs:
  build:
    name: Build on ${{ matrix.platform }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    env:
      PROJECT_NAME: ${{ github.event.repository.name }}
      # 全局禁用 Xmake 交互 + 强制使用系统库
      XMAKE_INTERACTIVE: 0
      XMAKE_COLORTERM: none
      XMAKE_PACKAGE_ENABLE: 0
      # 指定系统库路径
      C_INCLUDE_PATH: /usr/include:/usr/include/musl
      LIBRARY_PATH: /usr/lib:/usr/lib/musl:/usr/lib/x86_64-linux-gnu:/usr/lib/aarch64-linux-gnu

    strategy:
      matrix:
        include:
          # 原有 Linux 版本（glibc）
          - os: ubuntu-latest
            platform: Linux
            arch: x64
          - os: ubuntu-24.04-arm
            platform: Linux
            arch: arm64
          # 新增 Linux musl 版本（静态编译）
          - os: ubuntu-latest
            platform: Linux
            arch: x64-musl
          - os: ubuntu-24.04-arm
            platform: Linux
            arch: arm64-musl
          # 原有 macOS/Windows 版本
          - os: macos-15-intel
            platform: macOS
            arch: Intel
          - os: macos-latest
            platform: macOS
            arch: arm64
          - os: windows-latest
            platform: Windows
            arch: x64
          - os: windows-11-arm
            platform: Windows
            arch: arm64

    steps:
      - name: Checkout Git Repository
        uses: actions/checkout@v5

      - name: Setup Xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: ~3
          actions-cache-folder: ".xmake-cache"
          build-cache-key: "${{ matrix.platform }}_${{ matrix.arch }}"
          build-cache: true
          package-cache: false  # 禁用包缓存，强制用系统库

      # x64-musl 专用工具链 + 系统级 pcre2 静态库
      - name: Install Musl & System Dependencies (x64-musl)
        if: matrix.arch == 'x64-musl'
        run: |
          sudo apt update && sudo apt install -y --no-install-recommends \
            musl-tools gcc-multilib musl-dev \
            libpcre2-dev libpcre2-8-0  # 系统级 pcre2 静态库（.a）
          # 强制 musl 编译器优先级
          sudo ln -sf /usr/bin/musl-gcc /usr/local/bin/gcc
          sudo ln -sf /usr/bin/musl-g++ /usr/local/bin/g++
          # 复制 pcre2 静态库到 musl 路径（确保能找到）
          sudo cp /usr/lib/x86_64-linux-gnu/libpcre2-8.a /usr/lib/x86_64-linux-musl/ || true

      # arm64-musl 专用工具链 + 系统级 pcre2 静态库
      - name: Install Musl & System Dependencies (arm64-musl)
        if: matrix.arch == 'arm64-musl'
        run: |
          sudo apt update && sudo apt install -y --no-install-recommends \
            musl-tools musl-dev \
            libpcre2-dev libpcre2-8-0  # 系统级 pcre2 静态库
          sudo apt remove -y gcc-multilib gcc-13-aarch64-linux-gnu || true
          # 强制 musl 编译器优先级
          sudo ln -sf /usr/bin/musl-gcc /usr/local/bin/gcc
          sudo ln -sf /usr/bin/musl-g++ /usr/local/bin/g++
          # 复制 pcre2 静态库到 musl 路径
          sudo cp /usr/lib/aarch64-linux-gnu/libpcre2-8.a /usr/lib/aarch64-linux-musl/ || true

      # 核心：完全绕开 Xmake 包管理，强制使用系统库构建
      - name: Build & Package Project
        run: |
          # 提取纯架构
          ARCH_PURE="${{ matrix.arch }}"
          ARCH_PURE=${ARCH_PURE/-musl/}
          
          # 彻底清理 Xmake 配置和缓存
          xmake clean -a
          xmake f --clean
          
          if [[ "${{ matrix.arch }}" == *-musl ]]; then
            # 强制指定编译器和静态链接标志
            export CC=musl-gcc
            export CXX=musl-g++
            export CFLAGS="-static -fPIC -Os -I/usr/include/pcre2"
            export CXXFLAGS="-static -fPIC -Os -I/usr/include/pcre2"
            # 显式链接系统 pcre2 静态库
            export LDFLAGS="-static -static-libgcc -static-libstdc++ -lm -lc -lpcre2-8"
            
            # 配置 Xmake：禁用包自动安装 + 仅用系统库
            xmake f --yes \
              --plat=linux \
              --arch=${ARCH_PURE} \
              --mode=release \
              --cxflags="${CFLAGS} ${CXXFLAGS}" \
              --ldflags="${LDFLAGS}" \
              --linkdirs="/usr/lib/musl:/usr/lib/x86_64-linux-musl:/usr/lib/aarch64-linux-musl" \
              --includedirs="/usr/include/pcre2:/usr/include/musl" \
              --pkg_searchdirs="" \  # 禁用包搜索，仅用系统库
              --no-packages  # 核心：禁用 Xmake 自动安装包
          else
            # 原有逻辑（glibc）
            xmake f --yes --mode=release
          fi
          
          # 构建（禁用所有包相关逻辑）
          xmake build --yes --verbose --no-packages
          xmake package --yes --verbose

      - name: Verify Static Link (musl only)
        if: contains(matrix.arch, 'musl')
        run: |
          BIN_PATH=$(find build/linux/${ARCH_PURE}/release -name "${PROJECT_NAME}" -type f)
          if [ -f "${BIN_PATH}" ]; then
            echo "✅ Binary found: ${BIN_PATH}"
            file ${BIN_PATH} | grep -i "statically linked" && echo "✅ Static link success"
            ldd ${BIN_PATH} || echo "✅ No dynamic dependencies (musl static confirmed)"
            # 验证包含 pcre2 静态依赖
            nm ${BIN_PATH} | grep -i pcre2_ && echo "✅ pcre2 linked statically"
          else
            echo "❌ Binary not found!"
            exit 1
          fi

      - name: Upload Artifact
        if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            build/packages/*.zip
            build/packages/*.tar.gz
          if-no-files-found: error
          retention-days: 7
          compression-level: 9

  release:
    if: ${{ (github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && inputs.deploy_type == 'release') }}
    name: Upload to GitHub Release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    env:
      RELEASE_DIR: "artifacts"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Download All Artifacts
        uses: actions/download-artifact@v5
        with:
          path: ${{ env.RELEASE_DIR }}

      - name: Update dev Tag
        run: |
          git tag dev ${{ github.sha }}
          git push origin "refs/tags/dev" --force
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev
          name: Continuous dev Build
          body: |
            Automated build from commit ${{ github.sha }}
            - Added Linux musl static build (x64/arm64)
            - Fixed: Use system pcre2 static lib (bypass Xmake package manager)
            - Deploy type: ${{ github.event_name == 'push' && 'auto-release (push)' || inputs.deploy_type }}
          prerelease: true
          preserve_order: true
          files: |
            ${{ env.RELEASE_DIR }}/**/*.zip
            ${{ env.RELEASE_DIR }}/**/*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  format:
    if: ${{ (github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && inputs.deploy_type == 'release') }}
    name: Auto Format Code
    needs: release
    runs-on: ubuntu-latest
    timeout-minutes: 60

    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Setup Xmake
        uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: ~3
          actions-cache-folder: ".xmake-cache"

      - name: Format C/C++ files
        run: xmake format

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet; then
            echo "changed=0" >> $GITHUB_OUTPUT
          else
            echo "changed=1" >> $GITHUB_OUTPUT
          fi

      - name: Auto commit and push changes
        if: steps.check_changes.outputs.changed != 0
        uses: stefanzweifel/git-auto-commit-action@v6
        with:
          commit_message: "style(format): AUTO format code"
          branch: ${{ github.ref_name }}
          commit_options: "--no-verify --no-signoff"
          file_pattern: "*.h *.c"
          commit_author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          status_options: "--untracked-files=no"
          add_options: "-u"
          disable_globbing: true